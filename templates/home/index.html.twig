{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ name }}!</h1>

{% if user.isConnected %}
<a href=""> <button>Déconnexion</button></a> 
{% else %}
<button>Connexion</button>
    
{% endif %}
<h3>Liste de jeux :</h3>
{% for game, multi in games %}
    <ul>
    {# loop.index pour indéxer automatiquement, option : {{loop.index0}}, pour démarrer à 0, {{loop.index + 10}}, pour ajouter 10 à chanque index Pour faire des additions :  {{multi + 150}} #}
    <li> {{loop.index}}. {{game}} : {{multi }} {% if multi > 1 %}
    joueurs maximum
    {% else %}
       joueur maximum  
    {% endif %} 
    </li></ul>
{% endfor %}

</div>
{% endblock %}
