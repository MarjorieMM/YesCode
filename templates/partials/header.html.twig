

<header>

{# {% set user = {firstname : "Tom", lastname : "Durand", role : "admin", isConnected : true} %} #}

			<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

				<a class="navbar-brand" href="{{path("home")}}">
                    YesCode
                </a>

				<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>

				<div class="collapse navbar-collapse" id="navbarColor01">
					<ul class="navbar-nav mr-auto">

						<li class="nav-item">
							<a class="nav-link" href="{{path("home")}}">
                                <i class="fa fa-home"></i>
                                Accueil
							</a>
						</li>

						<li class="nav-item">
							<a class="nav-link" href="{{path("articles")}}">
                                <i class="fa fa-newspaper"></i>
                                Articles
							</a>
						</li>
   {% if user is defined and user is not null %}
						<li class="nav-item">
							<a class="nav-link" href="{{path('user_logout')}}">
                                <i class="fas fa-sign-in-alt"></i>
                               Déconnection
							</a>
						</li>
                        {% else %}
                          <li class="nav-item">
							<a class="nav-link" href="{{path('user_register')}}">
							<i class="fas fa-user-plus"></i>
                               Inscription
							</a>
						</li> 
                           <li class="nav-item">
							<a class="nav-link" href="{{path('user_login')}}">
                                <i class="fas fa-sign-out-alt"></i>
                               Connection
							</a>
						</li> 
{% endif %}

                    </ul>
 			   {% if user is defined and user is not null %}


                    <ul class="navbar-nav ml-auto">
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="fa fa-user"></i>
                               {{user.firstname | title}}
                               {{user.lastname | title}}
                            </a>
							<div class="dropdown-menu dropdown-menu-right">
								<a class="dropdown-item" href="#">Mon Profil</a>
								<a class="dropdown-item" href="#">Mes articles</a>
                                {# {% if user.role == "admin" %} #}
                                <a class="dropdown-item" href="#">Espace Admin</a>
                                {# {% endif %} #}
								
								<div class="dropdown-divider"></div>
								<a class="dropdown-item" href="{{path('user_logout')}}">Déconnexion</a>
							</div>
						</li>
					</ul>
					{% endif %} 
				</div>
			</nav>
			{# Systeme de FLASH en place #}

{% for label,messages in app.flashes %}
	<div class="container text-center my-5 ">
		<div class="alert alert-{{ label }}">
			{% for message in messages %}
				<p>{{message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}

{# Fin du FLASH #}
		</header>